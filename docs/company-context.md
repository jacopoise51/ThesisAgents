# Company / internship context (NDA-aware)

> ALL agents must read this before writing anything that touches the
> internship work. The "Cleared for thesis" list below is the ONLY
> internship-derived material that may appear in the document.
>
> If something is not on the cleared list, the writer must either:
>   (a) reference the public-literature equivalent instead, or
>   (b) flag it with `% TODO-CLEARANCE: <topic>` and stop.

## Internship summary (public-safe paraphrase)
<one paragraph the public could read — no system names, no parameters>

## Cleared for thesis
- <topic / fact / figure 1>
- <topic / fact / figure 2>
- <topic / fact / figure 3>

## NOT cleared — must not appear
- Specific system / product / vendor names from the company.
- Operational parameters (frequencies, power levels, modulation schemes
  tied to a real product, internal benchmark numbers).
- Customer or programme names.
- Internal toolchain names not present in public documentation.
- Anything from internal documents not explicitly cleared above.

## Substitution rules
- Replace internal system names with generic descriptors
  ("a tactical UHF link", "a software-defined radio platform").
- Replace internal benchmark numbers with public-literature equivalents
  and cite the public source.
- If a method is internal but the underlying technique is published,
  cite the published version and describe the technique generically.

## Approval log
- <YYYY-MM-DD>: <topic> cleared by <name / role>
- <YYYY-MM-DD>: <topic> cleared by <name / role>
